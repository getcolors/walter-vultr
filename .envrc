export DIRENV_WARN_TIMEOUT=20s

eval "$(devenv direnvrc)"
use devenv 2>/dev/null

# This tracked file contains no credentials. Provider, backend, and optional
# feature secrets are loaded from the ignored private file when it exists.
# Never export COLORS_PAR_PROFILE.
source_env_if_exists .envrc.private
